# Developer runtime settings. After azd provision, run scripts/create-dev-env.sh
# or scripts/create-dev-env.ps1 to create local .env from the selected azd environment.

# AI Gateway is the model and tool front door for this sample.
AZURE_AI_GATEWAY_ENDPOINT="https://<your-ai-gateway-host>/"
# MCP clients use this URL and send AZURE_AI_GATEWAY_API_KEY in the Api-Key header.
AZURE_AI_GATEWAY_GITHUB_MCP_ENDPOINT="https://<your-ai-gateway-host>/default/toolservers/github/mcp"
AZURE_AI_GATEWAY_API_KEY="<gateway-api-key-from-apiKeys-default-create-listSecrets-or-listValues>"
AZURE_AI_GATEWAY_MODEL="gpt-latest"
AZURE_AI_GATEWAY_MINI_MODEL="gpt-mini-latest"

# Foundry Toolbox registered by azd provision.
TOOLBOX_NAME="repo-digest-tools"

# GitHub repository enforced for every tool call.
GITHUB_REPOSITORY="microsoft/agent-framework"
